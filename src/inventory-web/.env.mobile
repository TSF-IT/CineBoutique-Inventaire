# Exemple à utiliser uniquement pour du débogage sans proxy TLS
# Remplacez <IP_VM> et <PORT> par l'adresse/port de votre API accessible depuis le mobile
# Attention : le scan caméra ne fonctionnera pas en HTTP car l'iPhone exige une connexion sécurisée.
VITE_API_BASE=http://<IP_VM>:<PORT>/api
